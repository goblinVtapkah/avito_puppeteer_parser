version: "3.9"

services:
  frontend:
    build: ./frontend
    restart: unless-stopped
    container_name: frontend
    env_file:
      - .env
    ports:
      - "0.0.0.0:3000:3000"
    volumes:
      - ./frontend/app/src:/app/src